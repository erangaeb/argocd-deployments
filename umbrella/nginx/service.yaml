apiVersion: v1
kind: Service
metadata:
  name: nginxservice
  namespace: rahasak
  labels:
    name: nginxservice
    backstage.io/kubernetes-id: hakka
spec:
  ports:
    - port: 80
  selector:
    app: nginx
  type: LoadBalancer
